<h1>Articles</h1>

Our blog has <%= Article.public_count %> articles and counting !!!

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
    <li>
      <a href="<%= article_path(article) %>" >
      <%= article.title %>
    </li>
    <% end %>
  <% end %>
</ul>

<%= link_to "New article madafaka", new_article_path %>